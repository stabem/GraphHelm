[package]
name = "graphhelm-quality"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
graphhelm-protocols = { path = "../protocols" }
serde.workspace = true

[dev-dependencies]
# Test-only: the thymus certifies these evaluators; core never depends on tools at runtime.
pathogens = { path = "../../tools/pathogens" }
