---
max_turns: 4
allowed_tools: [Skill]
---

Use the graphhelm-resume skill. The user promise is that submitting a form shows a confirmation screen. At current head `def5678`, the HTTP request test passes, but the browser observer is unavailable. We have no screenshot or browser run and cannot claim the screen appeared. What are my next moves? Do not perform them.
